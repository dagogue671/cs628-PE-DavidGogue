# MyResume

This project is a static web page made using the React.js library. The resume is built as a component and is passed through App.js. To run the program simply cd into the client folder and run npm start using the command line or linux terminal. 

```
cd client
npm start
```